// IE required polyfills, in this exact order
import 'es6-shim';

// Angular 2 polyfills
// (these modules are what are in 'angular2-polyfills' so that's not needed again later on)
import 'es6-promise';
//import 'angular2-polyfills';

// Angular 2 libs
import 'reflect-metadata';
import 'zone.js/dist/zone-microtask.min';

// Angular 2 deps
import 'rxjs';
